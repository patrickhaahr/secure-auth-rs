# Post-Quantum Cryptography Keys
# Generate with: cargo run --bin keygen -- server --output-dir ./keys
PQ_SECRET_KEY_PATH=keys/server_hybrid.sk
PQ_PUBLIC_KEY_PATH=keys/server_hybrid.pk

# Sender keys directory (for authorized third-party senders)
# Each sender needs their Ed25519 public key registered in the database
PQ_SENDERS_DIR=keys/senders